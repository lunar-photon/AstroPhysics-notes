\chapter{Angular Resolution: Baselines, Diffraction, and Pixel Sampling}

\section{Windows on the Universe: Why We Observe the Way We Do}
\label{sec:windows}

Chapters~1--4 treated the detection of a photon and the reduction of its flux, position, and motion as given. Before asking \emph{how sharply} we can see the sky, it is worth asking a more basic question: \emph{how do we see it at all?} Electromagnetic radiation from a cosmic source must first survive an $\sim 100\,\mathrm{km}$ trip through the Earth's atmosphere before it ever reaches a mirror, dish, or detector. Whether astronomers build an optical telescope on a mountaintop, a radio dish array spread across a continent, or an X-ray observatory in orbit is not a matter of taste --- it is dictated entirely by which photons the atmosphere actually lets through.

\begin{itemize}
	\item \textbf{Gamma rays and X-rays} ionize and are absorbed high in the atmosphere (this is, in fact, what protects the biosphere from them). No ground-based telescope can see this light directly; it must be observed from orbit (e.g.\ Chandra, NuSTAR).
	\item \textbf{Ultraviolet light} below $\sim 300\,\mathrm{nm}$ is absorbed by ozone ($\mathrm{O}_3$) in the stratosphere through electronic transitions. This is the same absorption that shields us from UV at ground level, and it makes ground-based UV astronomy essentially impossible below the ozone cutoff.
	\item \textbf{Visible light} ($\sim 350$--$800\,\mathrm{nm}$) falls in a fortunate spectral gap: it is too low in energy to drive the electronic transitions of $\mathrm{O}_2$ and $\mathrm{O}_3$ that absorb the UV, and too low in frequency to excite the vibrational--rotational transitions of $\mathrm{H}_2\mathrm{O}$, $\mathrm{CO}_2$, and $\mathrm{CH}_4$ that absorb the infrared. Rayleigh scattering off air molecules ($\propto \lambda^{-4}$) still preferentially scatters the blue end of this window (hence the daytime sky glow), but it does not extinguish the beam --- this is precisely why the \emph{optical window} exists, and why our eyes (and our telescopes) are tuned to it.
	\item \textbf{Infrared light} is chopped into narrow ``windows'' ($J, H, K, L, M$ bands, etc.) squeezed between strong telluric water-vapour and $\mathrm{CO}_2$ absorption bands. This is why infrared observatories sit at high, dry sites (Mauna Kea, Atacama) or fly above most of the water vapour, on aircraft, balloons, or in space (SOFIA, JWST).
	\item \textbf{Radio waves} pass through comparatively freely from $\sim 10\,\mathrm{MHz}$ up to hundreds of GHz --- the \textbf{radio window}. Below $\sim 10\,\mathrm{MHz}$, the ionosphere reflects radio waves back into space (the same effect that lets AM radio signals bounce around the globe), so the very longest wavelengths are again inaccessible from the ground. At the millimetre/submillimetre end, water vapour absorption dominates once more, which is why facilities such as ALMA are built at $5000\,\mathrm{m}$ altitude in the Atacama desert, above most of the water column.
\end{itemize}

This chapter concerns itself with the two atmospheric windows that dominate everyday astronomy: the \textbf{optical window} and the \textbf{radio window}. Crucially, these two windows favour two very different strategies for achieving high angular resolution. Radio astronomers can freely combine antennas separated by continental (or even intercontinental) distances, because the atmosphere barely perturbs radio wavefronts over such scales; optical astronomers, by contrast, are largely restricted to a single, monolithic (or segmented) aperture, whose resolution is set by wave diffraction and, from the ground, further degraded by atmospheric turbulence even though the light itself is not absorbed. Both strategies, and the detectors that ultimately record the result, are the subject of this chapter.

\section{The Chain of Sharpness: What Sets a Telescope's Angular Resolution}

Once light has survived its journey through the atmosphere, a chain of four physical and geometric effects determines how much fine spatial detail we can actually recover from it. Each link in this chain is the subject of one section of this chapter:
\begin{enumerate}[leftmargin=2em]
	\item \textbf{Interferometric baseline geometry} (Section~\ref{sec:baselines}): if a single mirror is replaced by an array of separated antennas, the effective aperture becomes the physical straight-line chord separation between them --- an idea that works spectacularly well in the radio window.
	\item \textbf{Telescope optics and focal scaling} (Section~\ref{sec:optics}): for a conventional single-aperture telescope, we first need the purely geometric mapping that converts an angle on the sky into a physical displacement in the focal plane, since that displacement is what a detector actually measures.
	\item \textbf{Diffraction wave optics} (Section~\ref{sec:diffraction}): no matter how perfectly a mirror or lens is figured, the finite size of the aperture imposes a fundamental wave-optical blur (Fraunhofer diffraction) through single slits, $N$-slit gratings, and circular/annular apertures (the Airy disk, chromatic resolving power, and resolution criteria) --- this sets the ultimate resolution any telescope can achieve.
	\item \textbf{Detector pixelization and sampling} (Section~\ref{sec:sampling}): the diffraction blur is only useful if the detector samples it finely enough; this trade-off is governed by the Nyquist--Shannon sampling theorem.
\end{enumerate}
For ground-based optical telescopes, one further complication reappears at the end of the chapter (Section~\ref{sec:seeing}): the atmosphere may be \emph{transparent} in the optical window, but it is not \emph{still} --- turbulent pockets of air with fluctuating refractive index blur the image well beyond the diffraction limit, which is precisely what Adaptive Optics ($\mathrm{AO}$) is built to correct.

\section{Interferometric Baselines: Chord Distance Between Ground Stations}
\label{sec:baselines}

The radio window identified above is where the idea of synthesizing a huge aperture out of small, widely separated pieces works best, since the atmosphere leaves radio wavefronts largely undisturbed over kilometre-to-continent scales. In Very Long Baseline Interferometry (VLBI) and connected-element radio interferometers (such as the GMRT or ALMA), the physical separation between two antennas defines the interferometer \emph{baseline}.

\subsection{Central Angle on a Spherical Earth}

Let two observatories be located at geodetic coordinates $(\phi_1, \lambda_1)$ and $(\phi_2, \lambda_2)$ on a spherical Earth of mean radius $R_E \approx 6371.0\,\mathrm{km}$. In the Earth-centered Cartesian coordinate frame:
\begin{equation}
	\hat{\mathbf r}_i = \begin{pmatrix} \cos\phi_i\cos\lambda_i \\ \cos\phi_i\sin\lambda_i \\ \sin\phi_i \end{pmatrix}, \qquad i \in \{1, 2\}.
	\label{eq:earth_station_vector}
\end{equation}

\begin{figure}[htbp]
	\centering
	\begin{minipage}[c]{0.38\textwidth}
		\begin{equation}
		\hat{\mathbf{r}}_{\rm geodetic} = 
		\begin{pmatrix}
		\cos\phi\cos\lambda\\
		\cos\phi\sin\lambda\\
		\sin\phi
		\end{pmatrix}
		\end{equation}
	\end{minipage}
	\hfill
	\begin{minipage}[c]{0.58\textwidth}
		\centering
		\includegraphics{fig_ch5_earth_geodetic_vector.pdf}
	\end{minipage}
	\caption{Geodetic coordinate definition on a spherical Earth with latitude $\phi$ and longitude $\lambda$.}
	\label{fig:earth_geodetic_vector}
\end{figure}

The angular separation $\gamma$ subtended by the two stations at the Earth's center is determined by their dot product:
\begin{align}
	\cos\gamma &= \begin{pmatrix} \cos\phi_1\cos\lambda_1 & \cos\phi_1\sin\lambda_1 & \sin\phi_1 \end{pmatrix} \begin{pmatrix} \cos\phi_2\cos\lambda_2 \\ \cos\phi_2\sin\lambda_2 \\ \sin\phi_2 \end{pmatrix} \notag\\
	&= \sin\phi_1\sin\phi_2 + \cos\phi_1\cos\phi_2\cos(\lambda_2-\lambda_1).
	\label{eq:earth_central_angle}
\end{align}

\subsection{Straight-Line Chord Baseline Geometry}

The straight-line (shortest distance in 3D) between two stations is given by measuring the \emph{chord distance} $C$ passing through the Earth's interior:

\begin{figure}[htbp]
    \centering
    \begin{minipage}[c]{0.42\textwidth}
        \includegraphics[width=\linewidth]{obs_vlbi_ggos_web_v2-1024x949.png}
    \end{minipage}
    \hfill
    \begin{minipage}[c]{0.54\textwidth}
    \centering
    \includegraphics{fig_ch5_vlbi_chord_geometry.pdf}
    \end{minipage}
    \caption{Global VLBI network (left) and the chord baseline geometry $C = 2R_E\sin(\gamma/2)$ through the Earth's interior (right).}
    \label{fig:vlbi_chord_geometry}
\end{figure}

The straight-line chord distance is therefore:
\begin{keyresult}
	\begin{equation}
		C = 2R_E\sin\left(\frac{\gamma}{2}\right) = R_E\sqrt{2(1-\cos\gamma)}.
		\label{eq:chord_baseline_formula}
	\end{equation}
\end{keyresult}

\subsection{Derivation of the Baseline Interferometer Angular Resolution Limit}

To understand how an interferometer synthesizes high angular resolution from two antennas separated by a physical baseline $B$, consider two distinct astronomical point sources on the sky, \textbf{Source 1} and \textbf{Source 2}, observed simultaneously at observing wavelength $\lambda$.

\begin{figure}[htbp]
\centering
\includegraphics{fig_ch5_interferometer_two_sources.pdf}
\caption{Geometric optical path delays for two distinct sources observed by a two-element interferometer of baseline $B$. Wavefronts from Antenna 2 ($A_2$) strike the rays entering Antenna 1 ($A_1$) at right angles ($90^\circ$), defining the respective geometric lags $\Delta L_1 = B\sin\theta_1$ and $\Delta L_2 = B\sin\theta_2$.}
\label{fig:interferometer_two_sources}
\end{figure}

Let the directions to the two sources make angles $\theta_1$ and $\theta_2$ with respect to the baseline normal:
\begin{enumerate}
	\item \textbf{Geometric Path Lag for Source 1:} A plane wavefront from Source 1 arrives at Antenna 2 before Antenna 1. The extra path distance that the light must travel to reach Antenna 1 is:
	\begin{equation}
		\Delta L_1 = B\sin\theta_1.
		\label{eq:path_lag_source_1}
	\end{equation}
	\item \textbf{Geometric Path Lag for Source 2:} Similarly, a plane wavefront from Source 2 arrives at angle $\theta_2$, resulting in a geometric path lag to Antenna 1 of:
	\begin{equation}
		\Delta L_2 = B\sin\theta_2.
		\label{eq:path_lag_source_2}
	\end{equation}
	\item \textbf{Differential Path Difference Between the Two Sources:} The net difference in path delay between the signals arriving from the two sources across the baseline is:
	\begin{equation}
		\Delta(\Delta L) = \Delta L_2 - \Delta L_1 = B\left(\sin\theta_2 - \sin\theta_1\right).
		\label{eq:diff_path_difference}
	\end{equation}
	For closely spaced sources on the sky, let $\theta = \theta_2 - \theta_1$ denote their mutual angular separation. Using the small-angle approximation ($\sin\theta_2 - \sin\theta_1 \approx \theta_2 - \theta_1 = \theta$ for angles near the normal):
	\begin{equation}
		\Delta(\Delta L) = B\left(\sin\theta_2 - \sin\theta_1\right) \approx B\theta.
		\label{eq:diff_path_approx}
	\end{equation}
	\item \textbf{Interferometric Resolution Criterion:} In a radio/optical interferometer, incoming wavefronts are correlated to produce sinusoidal interference fringe patterns. For the interferometer to cleanly resolve (distinguish) Source 1 from Source 2, the fringe pattern of Source 2 must shift relative to that of Source 1 by at least \textbf{one full interference fringe cycle} (corresponding to a full phase shift of $\Delta\phi = 2\pi$), which requires an extra path difference of exactly one full wavelength $\lambda$:
	\begin{equation}
		B\left(\sin\theta_2 - \sin\theta_1\right) = B\theta = \lambda.
		\label{eq:interferometer_resolution_condition}
	\end{equation}
\end{enumerate}

Solving Eq.~\eqref{eq:interferometer_resolution_condition} for the limiting angular resolution $\theta_{\rm res}$:
\begin{keyresult}
	\begin{equation}
		\theta_{\rm res} = \frac{\lambda}{B}\quad [\text{radians}] = \frac{\lambda}{B} \times \frac{180 \times 3600}{\pi}'' = 206{,}265\left(\frac{\lambda}{B}\right)\quad [\text{arcseconds}].
		\label{eq:interferometric_resolution_formula}
	\end{equation}
\end{keyresult}
This fundamental equation demonstrates that interferometric resolution depends solely on the ratio of the observing wavelength $\lambda$ to the baseline separation $B$, allowing distributed arrays to synthesize effective apertures thousands of kilometers wide.

\subsection{Worked Example: Interferometric Resolution for the GMRT to Ooty Baseline}

The baseline length $B = C$ determines the synthesized angular resolution as derived above:
\begin{equation}
	\theta \simeq \frac{\lambda}{B}.
	\label{eq:interferometric_res_approx}
\end{equation}

\begin{example}[Interferometry Between GMRT and Ooty Radio Telescope]
	The coordinates $(\phi,\lambda)$ of the Giant Metrewave Radio Telescope (GMRT, Khodad) and Ooty Radio Telescope (ORT) are:
	\[
	\begin{aligned}
		\text{GMRT}:&\quad (19.09^\circ\text{N},\,74.05^\circ\text{E}),\\
		\text{Ooty}:&\quad (11.38^\circ\text{N},\,76.66^\circ\text{E}).
	\end{aligned}
	\]
	\begin{enumerate}
		\item \textbf{Baseline Distance:} Using Eq.~\eqref{eq:earth_central_angle}:
		\[
		\cos\gamma = \sin(19.09^\circ)\sin(11.38^\circ) + \cos(19.09^\circ)\cos(11.38^\circ)\cos(76.66^\circ - 74.05^\circ) \approx 0.9899.
		\]
		The corresponding straight-line chord distance with $R_E = 6371\,\text{km}$ is:
		\[
		C = 2R_E\sin\left(\frac{\gamma}{2}\right) \implies \boxed{C \simeq 904.5\,\mathrm{km}}.
		\]
		\item \textbf{Angular Resolution at $1000\,\mathrm{MHz}$:}
		At observing frequency $f = 1000\,\mathrm{MHz} = 10^9\,\mathrm{Hz}$, the observing wavelength is:
		\[
		\lambda = \frac{c}{f} = \frac{3 \times 10^8\,\mathrm{m/s}}{10^9\,\mathrm{Hz}} = 0.30\,\mathrm{m}.
		\]
		Taking the baseline as $B \simeq 904.5\,\mathrm{km} = 9.045 \times 10^5\,\mathrm{m}$:
		\[
		\theta \simeq \frac{\lambda}{B} = \frac{0.30}{9.045 \times 10^5} \simeq 3.32 \times 10^{-7}\,\mathrm{rad}.
		\]
		Converting to milliarcseconds ($1\,\text{rad} = \frac{180}{\pi} \times 3.6 \times 10^6\,\text{mas} \approx 2.06265 \times 10^8\,\text{mas}$):
		\[
		\boxed{\theta \simeq 3.32 \times 10^{-7}\left(\frac{180}{\pi}\right)(3.6 \times 10^6) \simeq 68.4\,\mathrm{mas}}.
		\]
	\end{enumerate}
\end{example}

\section{Telescope Optics: Cassegrain Geometry, Focal Length, and Scale Relations}
\label{sec:optics}

Away from the radio window, spreading antennas across a continent is not an option: at optical wavelengths the atmosphere itself sets a coherence scale of only tens of centimetres (Section~\ref{sec:seeing}), so a single figured mirror (or a phased, co-mounted segmented one, such as an ELT) is the only practical way to collect light. Before we can even talk about the diffraction limit of such a mirror, we need the purely geometric machinery that turns an angle on the sky into a position on a detector --- this is the job of the telescope's optical train, worked out below for the common Cassegrain design, after first surveying why reflectors are preferred and how the focused beam is actually routed to a detector.

\subsection{Reflectors versus Refractors}

A telescope's primary collecting element is either a lens (\emph{refractor}) or a mirror (\emph{reflector}). Nearly all modern research telescopes are reflectors, for reasons that are all consequences of the primary being a first-surface mirror rather than a bulk transmissive optic:
\begin{itemize}
	\item Light never passes through the mirror substrate, so there is no chromatic aberration and negligible bulk absorption (a lens disperses different wavelengths to different foci, and thick glass absorbs UV and parts of the IR).
	\item Only one surface must be figured to optical precision (a lens requires two), and a mirror can be made far lighter than an equal-aperture lens.
	\item A mirror can be mechanically supported across its entire back surface, whereas a lens can only be held at its edge --- a much less rigid, more gravity-sag-prone support for large apertures.
	\item Because the back surface carries no optical function, it can be fitted with actuators that actively deform the mirror to cancel gravitational sag or (partially) atmospheric distortion (\emph{active optics}, a slower, whole-mirror-shape cousin of the adaptive optics of Section~\ref{sec:seeing}).
\end{itemize}
The unavoidable price of a reflector is that the secondary mirror or the detector itself must be mounted \emph{in front of} the primary, in the converging beam, physically obstructing part of the aperture (Section~\ref{sec:spiders_domes}).

\subsection{Focus Configurations: Prime, Newtonian, Cassegrain, and Coud\'e}

The same primary mirror can route its light to a detector in several standard ways, distinguished by what (if anything) intercepts the beam before the primary focus:
\begin{itemize}
	\item \textbf{Prime focus:} The detector sits directly at the primary's own focal plane, with no secondary mirror at all. This is optically the simplest configuration and wastes no light on a second reflection, but the detector package itself obstructs the incoming beam and is awkward to service on a large telescope.
	\item \textbf{Newtonian:} A flat secondary mirror, inclined at $45^\circ$ just before the primary focus, folds the beam out through the side of the telescope tube --- convenient for smaller, amateur-scale reflectors.
	\item \textbf{Cassegrain:} A convex secondary reflects the converging beam back through a central hole in the primary (derived in detail below), keeping the instrument compactly mounted behind the primary mirror.
	\item \textbf{Coud\'e:} A train of additional folding mirrors (often routed along the telescope's mounting axes) carries the beam to a fixed focus in a separate, environmentally stable room, decoupled from the telescope's own motion. This sacrifices some light to extra reflections but allows very large, heavy, temperature-sensitive instruments (notably high-resolution echelle spectrographs) that could never be mounted directly on the moving telescope structure.
\end{itemize}
Whichever focus is used, the telescope mount itself must continuously drive the optical axis to counteract the diurnal motion derived in Chapters~1--2 (i.e.\ to track at the sidereal rate in Hour Angle, Eq.~\eqref{eq:hour_angle_formula}), whether by an equatorial mount aligned with the polar axis or by a computer-driven altitude-azimuth mount.

\subsection{Obstruction Effects and Observatory Domes}
\label{sec:spiders_domes}

The secondary mirror (or prime-focus detector) must be held in place by a support structure --- the \emph{spider} --- spanning the tube in front of the primary. Because the spider vanes are themselves a set of thin opaque strips inserted into the pupil, they modify the aperture transmission function $t(x,y)$ of Eq.~\eqref{eq:aperture_field_def} exactly as the slit boundaries did in Section~\ref{sec:diffraction}: by the same Fraunhofer machinery, thin linear obstructions produce their own linear diffraction pattern, oriented perpendicular to each vane, superposed on the stellar Airy pattern. These are the familiar four-pointed (or six-pointed) \emph{diffraction spikes} seen radiating from bright stars in reflector images; irregularities in the mirror's own figure can produce weaker spikes by the same mechanism. Finally, telescopes are typically enclosed in a rotating \emph{dome}, which protects the optics and instruments from wind, precipitation, and temperature swings (air currents inside an open tube degrade the effective seeing of Section~\ref{sec:seeing}), and blocks stray light from the surrounding site.

\subsection{Telescope Technologies Across the Electromagnetic Spectrum}

Section~\ref{sec:windows} identified which photons reach which vantage point; here we survey how each band is actually focused into an image, since ordinary front-surface mirrors --- perfectly adequate through the optical and much of the UV/IR --- eventually fail entirely as photon energy increases.

\paragraph{UV and IR.} Ultraviolet and infrared telescopes are optically identical in principle to optical reflectors (mirrors focusing light obey the same geometry regardless of wavelength), differing mainly in their reflective coatings, which must be optimized for reflectivity at the wavelength of interest. As established in Section~\ref{sec:windows}, they must additionally be flown in space (UV: AstroSat-UVIT; IR: JWST) or at high, dry sites, since the atmosphere itself is the limiting factor rather than the optics.

\paragraph{X-rays.} At X-ray energies, ordinary mirrors fail even in principle: photon energies now exceed the atomic binding energies of the mirror material, so the refractive index for X-rays is very slightly \emph{less} than unity, $n = 1-\delta$ with $\delta \sim 10^{-5}$--$10^{-6}$ (equivalently, the X-ray phase velocity in the medium slightly exceeds $c$). Ordinary near-normal-incidence reflectivity is essentially zero. However, exactly as light undergoes total internal reflection when travelling from a dense to a rare medium beyond a critical angle, X-rays undergo \emph{total external reflection} off a denser medium when the grazing angle is sufficiently shallow.

\begin{example}[Critical Grazing Angle for X-ray Reflection]
	Let $\alpha$ be the grazing angle, measured from the mirror surface (so the angle of incidence from the normal is $90^\circ - \alpha$). Applying Snell's law at the vacuum--mirror interface ($n_1 = 1 \to n_2 = 1-\delta$), total reflection sets in once the refracted ray would require $\sin\theta_{\rm refr} > 1$, i.e.\ once
	\[
	\sin(90^\circ - \alpha) = \cos\alpha \ge 1 - \delta.
	\]
	For the shallow grazing angles relevant here ($\alpha \ll 1$), expand $\cos\alpha \approx 1 - \alpha^2/2$:
	\[
	1 - \frac{\alpha_c^2}{2} = 1 - \delta \quad\Longrightarrow\quad \boxed{\alpha_c \approx \sqrt{2\delta}}.
	\]
	Below this critical grazing angle $\alpha_c$, X-rays reflect almost perfectly. This is the physical basis of the \emph{Wolter Type-I} grazing-incidence mirror assemblies (nested, shallow-angle paraboloid--hyperboloid shells) used by Chandra and XMM-Newton to focus soft X-rays.
\end{example}

\paragraph{Gamma rays.} At still higher (hard X-ray/gamma-ray) energies, reflectivity is too low at any angle of incidence, and focusing mirrors of any kind become impractical. Imaging instead relies on \emph{coded aperture masks}: a pattern of opaque and transparent elements placed in front of a position-sensitive detector casts a known shadow pattern that depends on source direction, and the sky image is recovered computationally by deconvolving the recorded shadow against the known mask pattern (used by AstroSat-CZTI and Swift-BAT).

\paragraph{Radio.} At the opposite end of the spectrum, a radio telescope is simply a metallic reflecting dish (solid or open mesh, since the mesh spacing need only be small compared to the (centimetre-to-metre scale) observing wavelength) with a receiver at the prime focus --- structurally the simplest telescope of all. This simplicity is precisely what makes the baseline interferometry of Section~\ref{sec:baselines} practical in a way that has no simple optical analogue: because radio periods ($\sim$ns--$\mu$s) are slow enough to digitize directly, each dish's signal can be independently timestamped against a precision atomic clock (Chapter~1's $\TAI$-referenced standards) and correlated computationally long after the fact --- exactly how VLBI synthesizes continental baselines without ever physically combining light in real time. Optical interferometry, by contrast, oscillates far too fast ($\sim 10^{14}\,\mathrm{Hz}$) to digitize directly, and so must combine the beams optically, in real time, using physical beam combiners.

\subsection{Cassegrain Reflector Telescope Geometry}

In a Cassegrain reflector telescope, incoming parallel light rays from infinity hit the concave primary mirror of diameter $D$ and focal length $F$. Before reaching the primary focus, a convex secondary mirror of diameter $b$ intercepts the converging cone at a distance $d$ from the primary, reflecting and focusing the beam through a central hole in the primary mirror onto a detector placed at distance $a$ behind the primary.

\begin{figure}[htbp]
\centering
\resizebox{0.95\textwidth}{!}{
\includegraphics{fig_ch5_cassegrain_geometry.pdf}
}
\caption{Cassegrain reflector telescope: similar-triangle geometry used to compute secondary mirror size $b$ and CCD focal distance $a$.}
\label{fig:cassegrain_geometry}
\end{figure}

\begin{example}[Calculating Dimensions $a$ and $b$ for a Cassegrain Telescope]
	Let the primary mirror have diameter $D = 30\,\mathrm{cm}$ and focal length $F = 2.0\,\mathrm{m}$. The secondary mirror is located at $0.75\,\mathrm{m}$ from the primary.
	\begin{enumerate}
		\item Since the secondary mirror is $0.75\,\mathrm{m}$ from the primary, it lies:
		\[
		F - 0.75\,\mathrm{m} = 1.25\,\mathrm{m}
		\]
		from the primary focus.
		\item Using similar triangles:
		\[
		\frac{b}{D} = \frac{1.25}{2.0} \implies b = 30\left(\frac{1.25}{2.0}\right) = 18.75\,\mathrm{cm}.
		\]
		\item The folded path from the secondary to the focus is $1.25\,\mathrm{m}$. Since the primary--secondary separation is $0.75\,\mathrm{m}$:
		\[
		a = 1.25 - 0.75 = 0.50\,\mathrm{m}.
		\]
	\end{enumerate}
\end{example}

\subsection{Optical Ray Diagram and Angular Resolution on the Focal Plane}

\begin{figure}[htbp]
    \centering
    \includegraphics{fig_ch5_telescope_ray_diagram.pdf}
    \caption{Ray diagram illustrating angular resolution of two point sources, highlighting the central chief rays while marginal rays are faded for visual clarity.}
    \label{fig:telescope_ray_diagram}
\end{figure}

\subsection{Plate Scale and Pixel Scale Formulations}

Every camera design boils down to one practical question: how many arcseconds of sky does a millimetre of detector real estate --- or, ultimately, a single pixel --- correspond to? The \textbf{plate scale} answers the first version of this question and the \textbf{pixel scale} answers the second; we will need both later in this chapter to decide whether a given detector is fine enough to keep up with the diffraction limit (Section~\ref{sec:sampling}). Both quantities are just restatements of the ray geometry already sketched in Fig.~\ref{fig:telescope_ray_diagram}, where a source at angle $\theta$ from the optical axis forms its image at focal-plane height $y$.

\paragraph{Exact Geometric Relation:} For a chief ray passing through the centre of an ideal thin lens (or the vertex of a mirror) of focal length $F$, elementary ray tracing gives the \emph{exact} relation between sky angle and focal-plane position:
\begin{equation}
	y = F\tan\theta \quad\Longrightarrow\quad \theta = \arctan\left(\frac{y}{F}\right).
	\label{eq:exact_angle_position_relation}
\end{equation}

\paragraph{Plate Scale ($P$):} Plate scale relates the angular field of view of a frame, $d\theta$, (typically measured in arcseconds), to the size of the detector, $ds$, either in terms of a physical dimension (typically millimetres) or per pixel, and is defined as the local derivative of Eq.~\eqref{eq:exact_angle_position_relation}:
\begin{equation}
	P \equiv \dv{\theta}{s} = \dv{\theta}{y} = \frac{1}{F}\cdot\frac{1}{1+(y/F)^2} = \frac{\cos^2\theta}{F}.
	\label{eq:plate_scale_exact}
\end{equation}
This exact form shows that the plate scale is, strictly, \emph{not} uniform across the field: since $\cos^2\theta$ decreases away from the optical axis, a millimetre near the edge of a wide field covers slightly fewer arcseconds than a millimetre near the centre. In practice, however, imaging fields of view rarely exceed a few degrees, so $\theta \ll 1\,\mathrm{rad}$ and $\cos^2\theta \to 1$ to extremely high precision (even at $\theta = 1^\circ$, $\cos^2\theta = 0.9994$). In this small-angle (paraxial) limit, Eq.~\eqref{eq:plate_scale_exact} reduces to $1/F$, and $\theta$ itself becomes directly proportional to $y$ (Eq.~\eqref{eq:exact_angle_position_relation} linearizes to $\theta \approx y/F$), so the plate scale can equally well be read off as the simple ratio of the two rather than a derivative. This is the working form used throughout the rest of this chapter:
\begin{keyresult}
	\begin{equation}
		P = \dv{\theta}{s} = \frac{\theta}{y},
		\label{eq:plate_scale_def}
	\end{equation}
\end{keyresult}
where $\theta$ is measured in arcsec ($=\frac{1}{3600}^\circ$). This is why a telescope's plate scale is normally quoted as a single number (arcsec/mm) rather than a function of position on the detector: outside of dedicated wide-field survey cameras, the $\cos^2\theta$ correction is negligible.

\paragraph{Pixel Scale ($\theta_{\rm pix}$):} The pixel scale is the angular extent corresponding to a single physical pixel --- it is what tells us, in practice, how many arcseconds of sky get dumped into one pixel's worth of charge --- and can be defined as:
\begin{equation}
	\theta_{\rm pix} = \frac{y_{\rm pix}}{F} = \frac{p}{F}.
	\label{eq:pixel_scale_def}
\end{equation}
From geometry:
\begin{align}
    \tan\theta \approx \theta = \frac{y}{F} \implies \frac{1}{F} = \frac{\theta}{y},
\end{align}
which relates the two scales.

\begin{keyresult}
	For a focal length $F$, a pixel of physical size $p$ subtends the angle:
	\begin{equation}
		\theta_{\rm pix} \simeq \frac{y}{F}\bigg\lvert_{y=p}.
		\label{eq:pixel_scale_lvert}
	\end{equation}
	With $p$ in metres (or $\mu\mathrm{m}$) and $F$ in metres:
	\begin{equation}
		\text{Pixel Scale} = \frac{y}{F}\bigg\lvert_{y=p} \times \frac{180 \times 3600}{\pi}'' = \frac{206{,}265 \times p\,[\mathrm{mm}]}{F\,[\mathrm{mm}]}\quad [\text{arcsec/pixel}].
		\label{eq:pixel_scale_boxed_result}
	\end{equation}
\end{keyresult}

\begin{example}[Pixel Scale for a CCD with $20\,\mu\mathrm{m}$ Pixels]
	With pixel size $p = 20\,\mu\mathrm{m} = 20 \times 10^{-6}\,\mathrm{m}$ and telescope focal length $F = 2\,\mathrm{m}$:
	\[
	\theta_{\rm pix} = \frac{y}{F}\bigg\lvert_{y=p} = \frac{20 \times 10^{-6}\,\mathrm{m}}{2\,\mathrm{m}} = 1.0 \times 10^{-5}\,\mathrm{rad}.
	\]
	Using $1\,\mathrm{rad} = \frac{180 \times 3600}{\pi}'' = 206265''$:
	\[
	\boxed{\theta_{\rm pix} = \frac{y}{F}\bigg\lvert_{y=p} \simeq 2.06''/\mathrm{pixel}}.
	\]
\end{example}

\section{Diffraction Wave Optics: Superposition, Apertures, and Resolution Limits}
\label{sec:diffraction}

The two-antenna argument of Section~\ref{sec:baselines} introduced the central concept of wave interference: two point-like collectors separated by a baseline $B$ produce an interference fringe pattern, and distinguishing two astronomical sources requires shifting that fringe by one full cycle ($\theta_{\rm res} \sim \lambda/B$). A single telescope aperture is the continuous extension of this exact principle: rather than two discrete antennas, an entire two-dimensional continuum of points across the primary mirror interferes with itself. This self-interference of waves is the domain of \textbf{Fraunhofer diffraction}, and it sets the fundamental, wave-optical resolution limit of any telescope.

\paragraph{The Physical Picture:}
Light from a distant star arrives at the top of the telescope as a uniform plane wave. If light were purely a stream of geometric rays, a perfect parabolic mirror would focus this light into an infinitely small mathematical point. However, light is an electromagnetic wave. When a planar wavefront is clipped by a finite aperture of diameter $D$, it inevitably bends and spreads into an angular pattern. Even with perfectly figured optics in space (such as the Hubble Space Telescope or JWST), wave diffraction smears a stellar point source into an extended \textbf{Point Spread Function} (PSF). 

In this section, we build this wave-optical diffraction pattern step by step: first establishing the linear superposition principle, analyzing the baseline 1D single slit and the $N$-slit spectroscopic grating, rigorously deriving the 2D circular Airy disk and the origin of its famous $1.22$ geometric factor, and finally comparing the standard resolution criteria for astronomical observations.

\subsection{Physical Principle: Diffraction as Linear Superposition of Wavelets}

Diffraction is a direct consequence of the \textbf{linear Superposition Principle} governing wave propagation. Under the \textbf{Huygens--Fresnel principle}, every unobstructed point in an illuminated aperture acts as a coherent source of an outgoing spherical secondary wavelet. The total electric field observed at any downstream focal-plane position is simply the coherent linear sum (surface integral) of all these secondary wavelets, each weighted by the optical path length it has travelled.

In telescope optics, the field across the pupil plane is structured in three steps:
\begin{enumerate}
	\item \textbf{Incident Starlight Wave:} Light from an unresolved distant star arrives as a uniform, planar wavefront of amplitude $E_{\rm inc}(x,y) = E_0 = \text{const}$.
	\item \textbf{Pupil Transmission Mask $t(x,y)$:} The physical opening (mirror boundary, rectangular slit, grating grooves, or secondary obstruction) defines a spatial transmission mask: $t=1$ where light enters unobstructed, and $t=0$ where blocked by baffles or spiders.
	\item \textbf{Exit Pupil Field $A(x,y)$:} The field immediately behind the aperture is the product:
	\begin{equation}
		A(x,y) = E_{\rm inc}(x,y) \cdot t(x,y) = \begin{cases}
			E_0, & \text{inside open aperture}, \\
			0, & \text{outside (blocked)}.
		\end{cases}
		\label{eq:aperture_field_def}
	\end{equation}
\end{enumerate}

\begin{figure}[htbp]
\centering
\includegraphics{fig_ch5_huygens_superposition_principle.pdf}
\caption{The physical foundation of diffraction: an incident star plane wave is clipped by the aperture mask $t(x,y)$. Every point in the open pupil emits a secondary Huygens wavelet with geometric path lag $\Delta L = y\sin\theta$, superposing constructively or destructively in the far field.}
\label{fig:huygens_superposition_principle}
\end{figure}

Consider an observation direction in spherical-polar angles about the optical axis $\hat z$:
\begin{equation}
	\hat{\mathbf k} = (\sin\theta\cos\phi,\ \sin\theta\sin\phi,\ \cos\theta),
	\label{eq:khat_spherical}
\end{equation}
where $\theta$ is the off-axis angular displacement and $\phi$ is the azimuth. Relative to the origin $(0,0,0)$, a wavelet emitted from aperture position $(x,y,0)$ travels an extra path distance $\Delta L = \mathbf r \cdot \hat{\mathbf k} = x\sin\theta\cos\phi + y\sin\theta\sin\phi$.

In astronomical imaging, all angles of interest are extremely small ($\theta \ll 1\,\mathrm{rad}$, typically arcseconds). Defining the Cartesian angular components on the sky:
\[
\theta_x \equiv \theta\cos\phi, \qquad \theta_y \equiv \theta\sin\phi,
\]
the small-angle path delay simplifies to $\Delta L \approx x\theta_x + y\theta_y$, corresponding to a phase shift $\Delta\Phi = k\Delta L = \frac{2\pi}{\lambda}(x\theta_x + y\theta_y)$. 

Summing all wavelets across the aperture yields the fundamental \textbf{Fraunhofer Fourier integral}:
\begin{keyresult}
	\begin{equation}
		E(\vec{k}) = \iint_{\mathbb{R}^2} A(\vec{r})\, e^{-i \vec{k} \cdot \vec{r}} \, d^2r, \qquad I(\vec{\theta}) = |E(\vec{\theta})|^2,
		\label{eq:fourier_optics_integral}
	\end{equation}
	where $\vec{k} = \left(\frac{2\pi}{\lambda}\theta_x, \, \frac{2\pi}{\lambda}\theta_y\right)$ is the angular spatial frequency vector on the sky.
\end{keyresult}
In physical terms, the complex electric field amplitude at the telescope focal plane is simply the two-dimensional spatial Fourier transform of the entrance pupil transmission mask.

\subsection{Single-Slit Diffraction: The 1D Cancellation Baseline}

To build intuition before treating the two-dimensional circular mirror, consider first a one-dimensional rectangular slit of width $a$ illuminated normally by monochromatic light of wavelength $\lambda$.

\paragraph{Geometric Zone-Pairing Mechanism:}
Why does a diffraction pattern have dark fringes? Subdivide the slit into two equal halves: the upper half $[0, a/2]$ and the lower half $[-a/2, 0]$. For every point $x$ in the lower half, there exists an identical corresponding point $x + a/2$ in the upper half. The geometric path difference between these paired rays at diffraction angle $\theta$ is $\Delta L = (a/2)\sin\theta$.

Complete destructive cancellation occurs across the entire slit simultaneously when this path difference equals half a wavelength ($\lambda/2$):
\begin{equation}
	\frac{a}{2}\sin\theta = \frac{\lambda}{2} \implies a\sin\theta = \lambda \implies \theta_{\rm min} \approx \frac{\lambda}{a}.
	\label{eq:single_slit_minimum}
\end{equation}
Notice that for a 1D slit of uniform height, the geometric factor multiplying $\lambda/a$ is exactly $\mathbf{1.00}$.

\paragraph{Analytical Fourier Integration:}
Integrating the continuous wavelet field across the slit from $x = -a/2$ to $+a/2$:
\begin{equation}
	E(\theta) = \int_{-a/2}^{a/2} \frac{E_0}{a} e^{-i k x \sin\theta} \, dx = E_0 \left[ \frac{e^{-i k (a/2)\sin\theta} - e^{i k (a/2)\sin\theta}}{-i k a \sin\theta} \right] = E_0 \frac{\sin\beta}{\beta},
\end{equation}
where $\beta \equiv \frac{\pi a\sin\theta}{\lambda} \approx \frac{\pi a\theta}{\lambda}$. The observable intensity is:
\begin{keyresult}
	\begin{equation}
		I(\theta) = I_0 \operatorname{sinc}^2(\beta) = I_0 \left[ \frac{\sin\left(\frac{\pi a\sin\theta}{\lambda}\right)}{\frac{\pi a\sin\theta}{\lambda}} \right]^2.
		\label{eq:single_slit_intensity}
	\end{equation}
\end{keyresult}
The first null occurs where $\beta = \pi$, giving $\sin\theta_1 = \lambda/a \implies \theta_1 \approx \lambda/a$. The central diffraction maximum contains $90.3\%$ of the total transmitted energy.

\subsection{$N$-Slit Diffraction Grating: Dispersion and Resolving Power}

An astronomical diffraction grating consists of $N$ identical parallel slits of width $a$, spaced at center-to-center pitch $d$, spanning a total physical illuminated width $W \approx N d$.

\begin{figure}[htbp]
\centering
\resizebox{0.95\textwidth}{!}{
\includegraphics{fig_ch5_n_slit_geometry.pdf}
}
\caption{Optical path delays across an $N$-slit diffraction grating. Rays diffracted at angle $\theta$ incur progressive lags $\Delta L_n = n d\sin\theta$ and focus onto the detector plane forming sharp spectral lines.}
\label{fig:n_slit_geometry}
\end{figure}

\subsubsection{Derivation of the Grating Intensity Profile}
Each slit produces a diffracted field modulated by the single-slit envelope $\frac{\sin\beta}{\beta}$. Adjacent slits introduce a mutual path delay $\Delta L = d\sin\theta$, corresponding to a phase delay $\delta = \frac{2\pi d}{\lambda}\sin\theta = 2\gamma$, where $\gamma \equiv \frac{\pi d}{\lambda}\sin\theta$.

Summing the coherent fields across all $N$ slits:
\begin{align}
	E(\theta) &= \left[E_0 \frac{\sin\beta}{\beta}\right] \sum_{n=0}^{N-1} e^{-i n \delta} = \left[E_0 \frac{\sin\beta}{\beta}\right] \left(\frac{1 - e^{-i N\delta}}{1 - e^{-i\delta}}\right) \notag\\
	&= E_0 \left(\frac{\sin\beta}{\beta}\right) \left[\frac{\sin(N\gamma)}{\sin\gamma}\right] e^{-i(N-1)\gamma},
	\label{eq:n_slit_field_sum}
\end{align}
Taking the absolute square $I(\theta) = |E(\theta)|^2$ yields the total intensity distribution:
\begin{keyresult}
	\begin{equation}
		I(\theta) = I_0 \underbrace{\left(\frac{\sin\beta}{\beta}\right)^2}_{\text{Single-slit envelope}} \times \underbrace{\left(\frac{\sin N\gamma}{\sin\gamma}\right)^2}_{\text{$N$-beam interference factor}}.
		\label{eq:grating_intensity_formula}
	\end{equation}
\end{keyresult}

\subsubsection{Key Properties: Maxima, Minima, and Resolving Power}
\begin{enumerate}
	\item \textbf{Principal Maxima (The Grating Equation):} 
	When $\gamma = m\pi$ ($m \in \mathbb{Z}$), all $N$ beams interfere in phase. The quotient $\frac{\sin(N\gamma)}{\sin\gamma} \to N$, concentrating the energy into extremely narrow, intense peaks with $I_{\rm peak} = N^2 I_0$. For incident angle $\theta_i$ and diffracted angle $\theta_m$, this yields the \textbf{Grating Equation}:
	\begin{equation}
		d\left(\sin\theta_m + \sin\theta_i\right) = m\lambda \qquad (m \in \mathbb{Z}).
		\label{eq:grating_equation}
	\end{equation}
	\item \textbf{Destructive Minima and Spectral Line Width:} 
	The principal peak drops to zero when the numerator vanishes, $N\gamma = N m\pi + \pi$. The resulting angular half-width between the peak and its first minimum is:
	\begin{equation}
		\Delta\theta_{\rm grating} = \frac{\lambda}{N d\cos\theta_m} = \frac{\lambda}{W_{\rm eff}},
		\label{eq:grating_angular_width}
	\end{equation}
	where $W_{\rm eff} = W\cos\theta_m$ is the projected width of the illuminated grating.
	\item \textbf{Chromatic Resolving Power ($\mathcal{R} \equiv \lambda/\Delta\lambda$):} 
	According to the Rayleigh criterion, two nearby spectral lines at $\lambda$ and $\lambda + \Delta\lambda$ are barely resolved when the peak of one falls on the first zero of the other:
	\begin{equation}
		m(\lambda + \Delta\lambda) = m\lambda + \frac{\lambda}{N} \implies m\Delta\lambda = \frac{\lambda}{N}.
	\end{equation}
\end{enumerate}
\begin{keyresult}
	\begin{equation}
		\mathcal{R} \equiv \frac{\lambda}{\Delta\lambda} = m N = \frac{W\left(\sin\theta_m + \sin\theta_i\right)}{\lambda}.
		\label{eq:grating_resolving_power}
	\end{equation}
\end{keyresult}
In astronomical spectroscopy, Eq.~\eqref{eq:grating_resolving_power} conveys a vital physical insight: \textbf{the resolving power of a spectrograph is simply the total optical path delay between the extreme edges of the grating, measured in wavelengths}. This is why high-resolution spectrographs (such as HARPS or ESPRESSO, reaching $\mathcal{R} > 100{,}000$ for exoplanet radial velocity searches) require very large physical gratings ($W \sim 20\text{--}80\,\mathrm{cm}$) operated in high diffraction orders ($m \gg 1$).

\subsection{Circular Apertures and the Airy Disk: Derivation of the $1.22$ Factor}

Astronomical telescopes do not use narrow rectangular slits; their primary collecting elements are circular mirrors or lenses of diameter $D$ (radius $R = D/2$). 

\paragraph{Physical Intuition: Why $1.22$ Instead of $1.00$?}
For a rectangular slit of width $a$, every slice across the slit has the exact same height and area. Pairing opposite halves $[-a/2, 0]$ and $[0, a/2]$ provides a 1-to-1 cancellation of equal-amplitude wavelets, achieving complete destructive interference at $\theta = \lambda/a$.

In a circular aperture of diameter $D$, however, the chord length varies continuously from zero at the rim ($y = \pm D/2$) to a maximum of $D$ across the center ($y = 0$). Slices near the central equator contain much more collecting area and emit significantly higher wavelet amplitude than slices near the edges. When the path delay between opposite edges reaches $\lambda$, the weaker wavelets from the narrow edges cannot completely cancel the stronger wavelets from the wide center. To achieve complete phase cancellation across the entire two-dimensional circular surface, the angle must open up further by approximately $22\%$ ($\theta_1 \approx 1.22\lambda/D$).

\subsubsection{Step-by-Step Derivation of the Airy Profile}
We now derive this result from first principles using linear wave superposition.

\paragraph{Step 1: 2D Surface Integral in Polar Coordinates.}
Let a circular pupil of radius $R = D/2$ be illuminated by a uniform plane wave of amplitude $E_0$. Choose polar coordinates $(r', \phi')$ in the pupil plane ($r' \in [0, D/2]$, $\phi' \in [0, 2\pi]$).

Consider the diffracted wave propagating toward an off-axis angle $\theta$. By circular symmetry, the pattern depends only on $\theta$ and is azimuthally symmetric. Choosing the observation direction along $\phi = 0$, a wavelet originating from position $(r', \phi')$ travels an extra path distance $\Delta L = r'\sin\theta\cos\phi' \approx r'\theta\cos\phi'$, giving phase lag $\Delta\Phi = k r'\theta\cos\phi'$ with wavenumber $k = 2\pi/\lambda$.

The total diffracted field amplitude is the coherent sum of all secondary wavelets over the circular area:
\begin{equation}
	E(\theta) = \int_0^{D/2} r'\,dr' \int_0^{2\pi} e^{-i k r' \theta \cos\phi'}\,d\phi'.
\end{equation}

\paragraph{Step 2: Azimuthal Integration via the Bessel Function $J_0$.}
Recall the integral representation of the zero-order Bessel function of the first kind:
\begin{equation}
	J_0(z) = \frac{1}{2\pi}\int_0^{2\pi} e^{-i z \cos\phi'}\,d\phi'.
\end{equation}
Substituting this into the field integral resolves the angular integral immediately:
\begin{equation}
	E(\theta) = 2\pi \int_0^{D/2} r' J_0(k r' \theta)\,dr'.
\end{equation}

\paragraph{Step 3: Radial Integration and the $J_1$ Identity.}
To evaluate the radial integral, introduce the dimensionless variable $x \equiv k r' \theta$, so that $r' = \frac{x}{k\theta}$ and $dr' = \frac{dx}{k\theta}$. The upper integration limit at $r' = D/2$ is:
\begin{equation}
	v \equiv k\left(\frac{D}{2}\right)\theta = \left(\frac{2\pi}{\lambda}\right)\left(\frac{D}{2}\right)\theta = \frac{\pi D \theta}{\lambda}.
\end{equation}
The field integral transforms to:
\begin{equation}
	E(\theta) = 2\pi \int_0^v \left(\frac{x}{k\theta}\right) J_0(x) \left(\frac{dx}{k\theta}\right) = \frac{2\pi}{(k\theta)^2} \int_0^v x J_0(x)\,dx.
\end{equation}
Using the standard Bessel recurrence and integration identity:
\begin{equation}
	\int_0^u x J_0(x)\,dx = u J_1(u),
\end{equation}
where $J_1$ is the first-order Bessel function of the first kind, we obtain:
\begin{equation}
	E(\theta) = \frac{2\pi}{(k\theta)^2} \cdot v J_1(v) = 2\pi\left(\frac{v}{k\theta}\right)^2 \frac{J_1(v)}{v} = \left[\pi\left(\frac{D}{2}\right)^2\right] \left[ \frac{2 J_1(v)}{v} \right].
\end{equation}
Here, $A_{\rm pup} = \pi(D/2)^2 = \frac{\pi D^2}{4}$ is the physical collecting area of the telescope aperture. In the on-axis limit ($\theta \to 0 \implies v \to 0$), the limit $\lim_{v\to 0}\frac{2J_1(v)}{v} = 1$, giving the on-axis amplitude $E(0) = E_0 A_{\rm pup}$.

\paragraph{Step 4: The Airy Intensity Profile.}
Squaring the normalized field amplitude yields the celebrated \textbf{Airy disk intensity profile}:
\begin{keyresult}
	\begin{equation}
		I(\theta) = I_0 \left[ \frac{2 J_1(v)}{v} \right]^2, \qquad \text{with} \quad v = \frac{\pi D \sin\theta}{\lambda} \approx \frac{\pi D \theta}{\lambda}.
		\label{eq:airy_disk_intensity}
	\end{equation}
\end{keyresult}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.9\linewidth]{Diffraction.pdf}
    \caption{Ray optics focusing through a circular entrance pupil and the resulting rotated Airy diffraction profile in the focal plane.}
    \label{fig:diffraction_pdf_diagram}
\end{figure}

\paragraph{Step 5: Origin of the $1.22$ Factor (The First Dark Ring).}
The first dark ring (first destructive minimum / zero of the Airy pattern) occurs where the intensity drops to zero, which requires:
\begin{equation}
	J_1(v_1) = 0 \qquad (v_1 > 0).
\end{equation}
The first positive root of the Bessel function $J_1$ is a standard transcendental value:
\begin{equation}
	v_1 \approx 3.83170597\dots
\end{equation}
Equating this root to the definition of $v = \frac{\pi D \theta_1}{\lambda}$ yields the angular radius of the first dark ring:
\begin{equation}
	\frac{\pi D \theta_1}{\lambda} = 3.831706 \implies \theta_1 = \frac{3.831706}{\pi} \frac{\lambda}{D} \approx 1.21966989\dots \frac{\lambda}{D} \approx 1.22 \frac{\lambda}{D}.
	\label{eq:airy_first_null}
\end{equation}
This is the rigorous mathematical origin of the factor $\mathbf{1.22}$ in telescope optics! 

In the small-angle regime, this sets the classic \textbf{Rayleigh Resolution Criterion}:
\begin{keyresult}
	\begin{equation}
		\theta_{\rm Rayleigh} \approx 1.22 \frac{\lambda}{D}\quad [\text{rad}] = 206{,}265 \times 1.22\frac{\lambda}{D}\quad [\text{arcsec}].
		\label{eq:rayleigh_criterion_result}
	\end{equation}
\end{keyresult}

\subsubsection{Encircled Energy and Annular Obstruction Effects}
\begin{itemize}
	\item \textbf{Encircled Energy Budget:} The central Airy disk (interior to the first null at $\theta_1 = 1.22\lambda/D$) encloses $\mathbf{83.8\%}$ of the total light energy. The first bright diffraction ring (peak at $v \approx 5.14$, with relative intensity $I/I_0 \approx 1.75\%$) encloses $7.2\%$, the second ring encloses $2.8\%$, and the remainder decays into outer rings.
	\item \textbf{Cassegrain Annular Obscuration:} In reflecting telescopes with a central secondary mirror of linear obscuration ratio $\epsilon = d_{\rm sec}/D$ (typically $\epsilon \sim 0.2\text{--}0.4$), Babinet's principle gives the annular diffracted field:
	\begin{equation}
		E_{\rm ann}(v) \propto \left[ \frac{2J_1(v)}{v} - \epsilon^2 \frac{2J_1(\epsilon v)}{\epsilon v} \right].
	\end{equation}
	The central obscuration slightly narrows the core diameter ($\theta_1 \approx 1.15\lambda/D$), but systematically transfers $6\text{--}8\%$ of the core energy into the bright outer diffraction rings. This reduces dynamic range and image contrast, a critical consideration for direct coronagraphic imaging of faint exoplanets around bright host stars.
\end{itemize}

\subsection{Resolution Criteria for Point-Source Imaging}

When observing two incoherent, equal-brightness point sources (e.g., a visual binary star system) separated by angular distance $\Delta\theta$, their intensities add linearly:
\begin{equation}
	I_{\rm tot}(\theta) = I\left(\theta + \frac{\Delta\theta}{2}\right) + I\left(\theta - \frac{\Delta\theta}{2}\right).
\end{equation}

\begin{figure}[htbp]
\centering
\resizebox{\textwidth}{!}{
\includegraphics{fig_ch5_rayleigh_sparrow_comparison.pdf}
}
\caption{Comparison of two-source resolution criteria: Rayleigh criterion ($19\%$ dip for slits, $26.5\%$ for Airy disk), Sparrow criterion (flat inflection plateau $\frac{d^2 I}{d\theta^2}=0$), and an unresolved profile.}
\label{fig:rayleigh_sparrow_comparison}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics{fig_ch5_rayleigh_diffraction_sinc.pdf}
    \caption{Diffraction patterns of two barely resolved point sources under the Rayleigh criterion, where the central maximum of one source coincides with the first minimum ($\theta_1 = 1.22\lambda/D$) of the second.}
    \label{fig:rayleigh_diffraction_sinc}
\end{figure}

Two criteria are commonly used to characterize resolution in astronomical systems:
\begin{enumerate}
	\item \textbf{Rayleigh Criterion ($\theta_{\rm Rayleigh}$):} 
	The central maximum of Source 1 coincides with the first diffraction minimum of Source 2 ($\Delta\theta = \theta_1$). 
	\begin{itemize}
		\item For a \textbf{1D slit} ($I \propto \operatorname{sinc}^2\beta$): $\Delta\theta = \lambda/a$, yielding a midpoint intensity dip of \textbf{$19\%$} ($I_{\rm mid} = 0.811 I_{\rm peak}$).
		\item For a \textbf{2D circular Airy disk} ($I \propto [2J_1(v)/v]^2$): $\Delta\theta = 1.22\lambda/D$, producing a deeper midpoint dip of \textbf{$26.5\%$} ($I_{\rm mid} \approx 0.735 I_{\rm peak}$).
	\end{itemize}
	Historically, the Rayleigh criterion represented the empirical threshold where the human eye looking through an eyepiece could reliably detect a brightness dip between two equal stellar images.
	\item \textbf{Sparrow Criterion ($\theta_{\rm Sparrow}$):} 
	The physical separation at which the central dip completely disappears, creating a flat plateau with zero second derivative at the midpoint:
	\begin{equation}
		\left.\frac{d^2 I_{\rm tot}}{d\theta^2}\right|_{\theta=0} = 0.
	\end{equation}
	This yields:
	\begin{itemize}
		\item For a \textbf{1D slit}: $\theta_{\rm Sparrow} \approx 0.83 \frac{\lambda}{a}$.
		\item For a \textbf{2D circular pupil}: $\theta_{\rm Sparrow} \approx 0.94 \frac{\lambda}{D}$.
	\end{itemize}
\end{enumerate}
In modern digital astrophysics, the Rayleigh limit is not a rigid barrier: with high Signal-to-Noise Ratio ($\mathrm{SNR}$) observations and accurate Point Spread Function ($\mathrm{PSF}$) modeling, astronomical deconvolution algorithms can cleanly separate overlapping sources down to the Sparrow limit and beyond.

\begin{example}[Resolving the Solar Sodium D-lines with a Grating Spectrograph]
	The Fraunhofer Sodium D-doublet consists of two absorption lines at $\lambda_1 = 589.59\,\mathrm{nm}$ and $\lambda_2 = 588.99\,\mathrm{nm}$ ($\Delta\lambda = 0.60\,\mathrm{nm}$, mean wavelength $\bar{\lambda} = 589.3\,\mathrm{nm}$).
	\begin{enumerate}
		\item \textbf{Required Resolving Power:}
		\[
		\mathcal{R} = \frac{\bar{\lambda}}{\Delta\lambda} = \frac{589.3\,\mathrm{nm}}{0.60\,\mathrm{nm}} \approx 982.
		\]
		\item \textbf{Required Grating Grooves in Order $m=2$:}
		\[
		N = \frac{\mathcal{R}}{m} = \frac{982}{2} = 491 \text{ illuminated grooves}.
		\]
		For a grating ruled at $600\,\text{grooves/mm}$, the required illuminated beam diameter is:
		\[
		W = N d = \frac{491}{600}\,\mathrm{mm} \approx 0.82\,\mathrm{mm}.
		\]
	\end{enumerate}
\end{example}

\begin{example}[Diffraction Resolution of a $30\,\mathrm{cm}$ Telescope at $\lambda = 500\,\mathrm{nm}$]
	For an optical telescope with aperture diameter $D = 0.30\,\mathrm{m}$ observing in the $V$-band ($\lambda = 500\,\mathrm{nm} = 500 \times 10^{-9}\,\mathrm{m}$):
	\[
	\theta_{\rm diff} = 1.22\frac{\lambda}{D} = 1.22 \times \frac{500 \times 10^{-9}\,\mathrm{m}}{0.30\,\mathrm{m}} \approx 2.033 \times 10^{-6}\,\mathrm{rad}.
	\]
	Converting to arcseconds ($1\,\mathrm{rad} = 206{,}265''$):
	\[
	\boxed{\theta_{\rm diff} \simeq 2.033 \times 10^{-6} \times 206{,}265'' = 0.42''}.
	\]
\end{example}

\section{Comparing Pixel Scale to the Diffraction Limit: Nyquist Sampling}
\label{sec:sampling}

We now have both halves of the picture needed to judge whether a telescope--camera combination is actually delivering the resolution it is capable of. Section~\ref{sec:optics} gave us the \textbf{pixel scale} $\theta_{\rm pix} = p/F$: how many arcseconds of sky a single physical pixel records. Section~\ref{sec:diffraction} gave us the \textbf{diffraction limit} $\theta_{\rm res}$ (e.g.\ $1.22\lambda/D$ for a circular aperture): the smallest true angular feature the optics can physically resolve. If the pixels are too coarse compared to this blur, fine spatial information is thrown away before it is even recorded, no matter how good the optics are; this is exactly the kind of question the Nyquist--Shannon sampling theorem was built to answer.

\subsection{The Nyquist Sampling Criterion}

This is a special case of a completely general result in signal processing, the \emph{Nyquist--Shannon sampling theorem}: a signal that contains no spatial variation finer than some highest frequency $f_{\max}$ can be perfectly reconstructed from discrete samples only if those samples are taken at intervals no coarser than $1/(2f_{\max})$ (samples any sparser cause distinct high-frequency features to become indistinguishable from one another --- \emph{aliasing}). Here, the diffraction blur $\theta_{\rm res}$ is the smallest angular feature the optical image can contain, i.e.\ the finest structure varies on an angular scale $\sim\theta_{\rm res}$, corresponding to a highest angular spatial frequency $f_{\max}\sim 1/\theta_{\rm res}$. The detector pixel grid is precisely a discrete sampling of the (continuous) focal-plane image, with sampling interval equal to the pixel scale. Applying the theorem directly:
\begin{keyresult}
	\begin{equation}
		\text{Pixel Scale} \le \frac{1}{2f_{\max}} \sim \frac{\theta_{\rm res}}{2}.
		\label{eq:nyquist_sampling_criterion}
	\end{equation}
\end{keyresult}
That is, there must be at least \textbf{two detector pixels across the resolution element} ($\mathrm{FWHM} \approx \theta_{\rm res}$).

\subsection{Configuration Critique: Comparison of Pixel Scale and Diffraction Limit}

In the telescope configuration analyzed above:
\begin{itemize}
	\item The pixel scale is:
	\[
	\text{Pixel Scale} = \frac{y}{F}\bigg\lvert_{y=p} = 2.06''/\mathrm{pixel}.
	\]
	\item The diffraction-limited resolution is:
	\[
	\theta_{\rm res} = 1.22\frac{\lambda}{D} = 0.42''.
	\]
\end{itemize}
The Nyquist sampling criterion requires:
\[
\text{Pixel Scale} \le \frac{\theta_{\rm res}}{2} = \frac{0.42''}{2} = 0.21'',
\]
which is \textbf{not respected}. 

Evaluating the ratio of the two quantities:
\begin{keyresult}
	\begin{equation}
		\frac{\text{Pixel Scale}}{\theta_{\rm res}} = \frac{2.06}{0.42} \simeq 4.9.
		\label{eq:pixel_diffraction_ratio}
	\end{equation}
\end{keyresult}
Thus, \textbf{one pixel covers almost five diffraction-limited resolution elements}, which represents a severely \emph{undersampled} configuration. High spatial frequencies are permanently lost to digital aliasing, and point sources cannot be astrometrically centroided to sub-diffraction accuracy.

\begin{table}[htbp]
	\centering
	\small
	\begin{tabular}{lp{5.5cm}p{6.0cm}}
		\toprule
		\textbf{Sampling Regime} & \textbf{Condition} & \textbf{Observational Consequences} \\
		\midrule
		\textbf{Critically Sampled} & $\text{Pixel Scale} \approx \theta_{\rm res}/2$ & Preserves all spatial frequencies without wasting detector area. \\
		\textbf{Undersampled} & $\text{Pixel Scale} > \theta_{\rm res}/2$ & Causes aliasing, jagged stellar profiles, and astrometric centroid degradation. \\
		\textbf{Oversampled} & $\text{Pixel Scale} \ll \theta_{\rm res}/2$ & Spreads light over excessive pixels, increasing total readout noise penalty $\sqrt{n_{\rm pix}}\sigma_{\rm RON}$. \\
		\bottomrule
	\end{tabular}
	\caption{Comparison of digital detector sampling regimes in astronomical imaging.}
	\label{tab:sampling_regimes}
\end{table}

\section{Atmospheric Seeing, PSF Profiles, and Adaptive Optics}
\label{sec:seeing}

We opened this chapter (Section~\ref{sec:windows}) by noting that the optical window is \emph{transparent} --- visible light is not absorbed on its way down. Transparency, however, is not the same as stillness. The same atmosphere that lets the light through is also turbulently mixing pockets of air at slightly different temperatures (and hence slightly different refractive indices) along the line of sight. A telescope's diffraction limit $\theta_{\rm res} = 1.22\lambda/D$ from Section~\ref{sec:diffraction} is only achieved if the incoming wavefront is genuinely flat when it reaches the aperture; from the ground, it rarely is.

\subsection{Atmospheric Seeing and the Fried Parameter ($r_0$)}

For ground-based optical telescopes, turbulent temperature and density variations in the Earth's atmosphere continuously perturb the local refractive index along the line of sight, so a wavefront that left a star perfectly flat arrives at the ground with small, randomly-varying extra path delays across its width. The \textbf{Fried coherence parameter} $r_0$ is defined as the transverse separation over which the root-mean-square phase difference across the wavefront reaches of order $1\,\mathrm{radian}$ (typically $r_0 \sim 10\text{--}20\,\mathrm{cm}$ in the $V$-band at premier mountain observatories): patches of the wavefront closer together than $r_0$ are still mutually coherent and interfere constructively as in an undisturbed Airy pattern, while patches separated by more than $r_0$ have essentially independent, uncorrelated phases. The full theory of how $r_0$ follows from the statistics of atmospheric (Kolmogorov) turbulence is beyond our present scope, but the qualitative consequence is immediate: an aperture larger than $r_0$ is effectively not one coherent aperture of diameter $D$ but a patchwork of $\sim(D/r_0)^2$ independently-phased sub-apertures of size $r_0$ each. For any telescope with aperture diameter $D > r_0$, the delivered angular resolution is therefore limited not by $D$ (as Eq.~\eqref{eq:rayleigh_criterion_result} would suggest) but by the effective aperture $r_0$, via the same diffraction relation with $D\to r_0$:
\begin{keyresult}
	\begin{equation}
		\theta_{\rm seeing} \approx 0.98\frac{\lambda}{r_0} \sim 0.5''\text{--}1.5''.
		\label{eq:seeing_resolution}
	\end{equation}
\end{keyresult}
Because $r_0 \propto \lambda^{6/5}$, seeing improves at infrared wavelengths ($\theta_{\rm seeing} \propto \lambda^{-1/5}$).

\subsection{Point Spread Function (PSF) Profiles: The Moffat Function}

Turbulent atmospheric scattering smears the theoretical Airy disk into a broader Point Spread Function (PSF). Astronomers parameterize this profile using the \textbf{Moffat function}:
\begin{equation}
	I(r) = I_0 \left[1 + \left(\frac{r}{\alpha}\right)^2\right]^{-\beta}, \qquad \mathrm{FWHM} = 2\alpha\sqrt{2^{1/\beta}-1},
	\label{eq:moffat_function}
\end{equation}
where $\beta \approx 2.5\text{--}4.5$ captures the extended scattering wings produced by Kolmogorov turbulence.

\subsection{Adaptive Optics (AO)}

Adaptive Optics systems restore diffraction-limited performance to ground-based telescopes in real time:
\begin{enumerate}
	\item A \textbf{Wavefront Sensor} (e.g., Shack--Hartmann lenslet array) measures instantaneous phase distortions $\Delta\phi(\mathbf r, t)$ at kHz frequencies using a bright Natural Guide Star (NGS) or Sodium Laser Guide Star (LGS, $\lambda = 589\,\mathrm{nm}$ resonant excitation at $\sim 90\,\mathrm{km}$ altitude).
	\item A real-time computer calculates the required phase conjugation.
	\item A \textbf{Deformable Mirror} (with hundreds to thousands of piezoelectric actuators) physically adjusts its surface profile to cancel out the atmospheric optical path difference.
\end{enumerate}
This restores the sharp $\theta \approx \lambda/D$ diffraction core on 8--30 meter class telescopes, providing space-quality resolution from the ground.


\section{Problem Set: Interferometric Baselines, Optics, and Resolution}

\subsection{Problem 1: GMRT to Ooty Radio Interferometer Baseline and Resolution}
\textbf{Problem Statement}: Find latitude and longitude of GMRT and
Ooty Radio Telescope from Google Maps. (a) What is the straight-line
distance between them? (b) If we do very-long-baseline interferometry
between these telescopes at $1000\text{ MHz}$, what is the best
resolution we can get?

\textbf{Essential Ideas}: The straight line (shortest distance in 3D) between them can be given by measuring the chord distance between them:

\begin{figure}[H]
    \centering
    \begin{minipage}[c]{0.40\textwidth}
        \includegraphics[width=\linewidth]{obs_vlbi_ggos_web_v2-1024x949.png}
    \end{minipage}
    \hfill
    \begin{minipage}[c]{0.55\textwidth}
    \centering
    \usetikzlibrary{angles, quotes, calc, decorations.pathreplacing}
    \includegraphics{fig_ch5_prob_chord_distance.pdf}
\end{minipage}
\end{figure}

The baseline length $B$ determines the
angular resolution approximately as
\[
\theta\simeq\frac{\lambda}{B}.
\]

\textbf{Detailed Solutions}:
\begin{itemize}

    \item[(a)] \textbf{Baseline Distance:}

    The coordinates $(\phi,\lambda)$ are approximately
    \begin{figure}[H]
    	\centering
    	\begin{minipage}[c]{0.35\textwidth}
    		\[
    		\begin{aligned}
    			\text{GMRT}:&\quad
    			(19.09^\circ\text{N},\,74.05^\circ\text{E}),\\
    			\text{Ooty}:&\quad
    			(11.38^\circ\text{N},\,76.66^\circ\text{E}).
    		\end{aligned}
    		\]
    	\end{minipage}
    	\begin{minipage}[c]{0.55\textwidth}
    		\includegraphics{fig_ch5_prob_sphere_arc.pdf}
    	\end{minipage}
	\end{figure}

    We can calculate the angle between these two coordinates by following the same method as before with $\delta=\phi$ and $\alpha=\lambda$:
    \begin{align}
            \cos\gamma&=\begin{pmatrix}
\cos\phi_1\cos\lambda_1&
\cos\phi_1\sin\lambda_1&
\sin\phi_1
\end{pmatrix}\begin{pmatrix}
\cos\phi_2\cos\lambda_2\\
\cos\phi_2\sin\lambda_2\\
\sin\phi_2
\end{pmatrix}\notag\\
&=
    \sin\phi_1\sin\phi_2+
    \cos\phi_1\cos\phi_2\cos(\lambda_2-\lambda_1).
    \end{align}

    The corresponding straight-line distance is
    \[
    C=2R_E\sin\left(\frac{\gamma}{2}\right),
    \qquad R_E=6371\text{ km}.
    \]
    This gives approximately
    \[
    \boxed{C\simeq904.5\text{ km}}.
    \]

    \item[(b)] \textbf{Angular Resolution:}

    At
    \[
    f=1000\text{ MHz}=10^9\text{ Hz},
    \]
    the observing wavelength is
    \[
    \lambda=\frac{c}{f}
    =\frac{3\times10^8}{10^9}
    =0.30\text{ m}.
    \]

    Taking the baseline as $B\simeq904.5\text{ km}
    =9.045\times10^5\text{ m}$,
    \[
    \theta\simeq\frac{\lambda}{B}
    =\frac{0.30}{9.045\times10^5}
    \simeq3.32\times10^{-7}\text{ rad}.
    \]

    Converting to milliarcseconds,
    \[
    \theta
    \simeq3.32\times10^{-7}
    \left(\frac{180}{\pi}\right)
    (3.6\times10^6)
    \simeq68.4\text{ mas}.
    \]
\end{itemize}

\subsection{Problem 2: Cassegrain Reflector Optics, Pixel Scale, and Sampling}
\textbf{Problem Statement}: The focal length of the primary mirror is $2\text{ m}$. (1) What are the lengths shown as ``b'' and ``a''? (2) We put a CCD at the focal plane with pixel size $20\ \mu\text{m}$. What is the pixel scale? (3) What is the resolution of this telescope at $500\text{ nm}$ wavelength? (4) Compare diffraction-limited resolution and pixel scale. Is this a good configuration?

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{
\includegraphics{fig_ch5_prob_cassegrain_optics.pdf}
}
\caption{Cassegrain reflector telescope}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics{fig_ch5_prob_chief_rays.pdf}
    \caption{Ray diagram illustrating angular resolution of two point sources, highlighting the central chief rays while marginal rays are faded for visual clarity.}
\end{figure}
Plate scale relates the angular field of view of a frame, $d\theta$, (typically measured in arcseconds), to the size of the detector, $ds$, either in terms of a physical dimension (typically millimetres) or per pixel and is defined as $P=\dv{\theta}{s}=\frac{\theta}{y}$, where $\theta$ is measured in arcsec $(=\frac{1}{3600}^{\circ})$.

The pixel scale is the angular extent corresponding to a single physical pixel, and can be defined as $\theta_{\rm pix}=\frac{y_{\rm pix}}{F}=\frac{p}{F}.$ From geometry, we have:
\begin{align}
    \tan\theta\approx\theta= \frac{y}{F}\implies\frac{1}{F}=\frac{\theta}{y}.
\end{align}
which relates the two scales.

\textbf{Detailed Solutions}:
\begin{itemize}

    \item[(1)] \textbf{Lengths $a$ and $b$:}

    The primary mirror has diameter
    \[
    D=30\text{ cm}
    \]
    and focal length
    \[
    F=2.0\text{ m}.
    \]
    Since the secondary mirror is $0.75\text{ m}$ from the primary,
    it lies
    \[
    F-0.75=1.25\text{ m}
    \]
    from the primary focus.

    Using similar triangles,
    \[
    \frac{b}{D}
    =\frac{1.25}{2.0},
    \]
    so
    \[
    b=30\left(\frac{1.25}{2.0}\right)
    =18.75\text{ cm}.
    \]

    The folded path from the secondary to the focus is $1.25\text{ m}$.
    Since the primary--secondary separation is $0.75\text{ m}$,
    \[
    a=1.25-0.75=0.50\text{ m}.
    \]
    
    \item[(2)] \textbf{Pixel Scale:}

    For a focal length $F$, a pixel of physical size $p$ subtends
    the angle
    \[
    \theta_{\rm pix}\simeq\frac{y}{F}\bigg\lvert_{y=p}.
    \]
    With
    \[
    p=20\,\mu\text{m}=20\times10^{-6}\text{ m},
    \qquad F=2\text{ m},
    \]
    we get
    \[
    \theta_{\rm pix}
    =\frac{20\times10^{-6}}{2}
    =1.0\times10^{-5}\text{ rad}.
    \]
    Using $1\text{ rad}=\frac{180\times 3600}{\pi }''=206265''$,
    \[
    \boxed{
    \theta_{\rm pix}\simeq2.06''/\text{pixel}.
    }
    \]

    \item[(3)] \textbf{Diffraction-Limited Resolution:}
\begin{figure}[H]
    \centering
    \includegraphics{fig_ch5_prob_diffraction_sinc.pdf}
\end{figure}
    The angular resolution of a circular aperture is approximately
    \[
    \theta_{\rm diff}
    =1.22\frac{\lambda}{D} \text{ rad.}
    \]
    For
    \[
    \lambda=500\text{ nm}=500\times10^{-9}\text{ m},
    \qquad D=0.30\text{ m},
    \]
    \[
    \theta_{\rm diff}
    =1.22\frac{500\times10^{-9}}{0.30}\times \frac{180\times 3600}{\pi }''
    \simeq2.03\times10^{-6}\times 206265''=0.42''.
    \]

    \item[(4)] \textbf{Comparison:}
    \begin{figure}[H]
        \centering
        \includegraphics[width=1\linewidth]{Diffraction.pdf}
    \end{figure}
    The pixel scale is
    \[
    \text{Pixel Scale}=\frac{y}{F}\bigg\lvert_{y=p}=2.06''/\text{pixel},
    \]
    whereas the diffraction-limited resolution is
    \[
    \theta_{\rm res}=1.22\frac{\lambda}{D}=0.42''.
    \]
    Nyquist sampling criterion requires
    $$\text{Pixel Scale} \le \frac{\theta_{\rm res}}{2}$$
    which is not respected. Their ratio is
    \[
    \frac{\text{Pixel Scale}}{\theta_{\rm res}}=\frac{2.06}{0.42}\simeq4.9.
    \]

    Thus one pixel covers almost five diffraction-limited resolution
    elements which is bad. 
\end{itemize}
